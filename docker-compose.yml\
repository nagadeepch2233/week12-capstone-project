version: "3.9"

services:
  backend:
    build: ./backend
    container_name: portfolio-backend
    ports:
      - ""
    restart: always

  frontend:
    build: ./frontend
    container_name: portfolio-frontend
    ports:
      - ""
    depends_on:
      - backend
    restart: always
