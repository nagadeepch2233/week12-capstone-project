import React from "react";

interface Props {
  category: string;
  content: string;
}

const PortfolioItem: React.FC<Props> = ({ category, content }) => {
  return (
    <div>
      <strong>{category}</strong>: {content}
    </div>
  );
};

export default PortfolioItem;
